import { ResultDatePipe } from './result-date.pipe';

describe('ResultDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ResultDatePipe();
    expect(pipe).toBeTruthy();
  });
});
